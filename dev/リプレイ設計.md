keisoku.py
lidarで指定時間ごとにファイルを分割して記録し、画像を作成する。

ベースはmakecsv.pyとautocsvtovis.py


- 言語はpython
- n分ごとにファイルを分割して保存し、ファイルごとに画像化
- ファイル名は"{開始時刻}-{終了時刻}"
    - 細かい時間を確認する必要がないなら、JSTやUTCではなくUnixtimeで表記
- オプション(n分分割など)の指定方式
    - 個人的にはconfファイルかyamlファイルを使いたいが、コマンド引数での指定のほうがいいか...?

